# Gemini API (required)
# Get your API key at https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Multiple API keys for rate limit fallback (comma-separated)
# When one key hits its daily quota, automatically rotates to the next
# GEMINI_API_KEYS=key1,key2,key3

# Optional: Dedicated TTS API key (for paid tier with higher rate limits)
# This is used specifically for text-to-speech, separate from video analysis
# GEMINI_TTS_API_KEY=your_tts_api_key

# Optional: Override default Gemini models
# Primary model for analysis, validation, and chat (default: gemini-3-pro-preview)
# GEMINI_MODEL=gemini-3-pro-preview
# Fast model for quick tasks like round detection (default: gemini-3-flash-preview)
# GEMINI_FAST_MODEL=gemini-3-flash-preview

# Optional: OpenAI as fallback provider (when Gemini is unavailable)
# OPENAI_API_KEY=your_openai_api_key
# OPENAI_ENABLED=true

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Google Cloud (only needed for video uploads)
# Without these, replay analysis works but video upload is disabled
# GOOGLE_CLOUD_PROJECT=your-project-id
# GCS_BUCKET_NAME=forging-uploads

# Local development authentication (choose one):
# Option 1 (recommended): Service account key file - no token expiration
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 2 (legacy): gcloud CLI impersonation - tokens expire hourly
# GCP_LOCAL_ACCOUNT=your-email@gmail.com
