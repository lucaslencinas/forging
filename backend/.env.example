# Gemini API
# Option 1: Single API key
GEMINI_API_KEY=your_gemini_api_key_here

# Option 2: Multiple API keys (comma-separated) for rate limit fallback
# When one key hits its daily quota, automatically rotates to the next
# GEMINI_API_KEYS=key1,key2,key3

# Google Cloud
GOOGLE_CLOUD_PROJECT=your-project-id
GCS_BUCKET_NAME=forging-uploads

# Local development: gcloud account to use for impersonation
# This allows you to keep a different default gcloud account while developing
GCP_LOCAL_ACCOUNT=your-email@gmail.com

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

OPENAI_API_KEY=myopenaikey

GEMINI_ENABLED=true
OPENAI_ENABLED=true