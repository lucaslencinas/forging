# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# AoE2 replay parsing
mgz==1.8.44

# CS2 demo parsing
demoparser2>=0.40.0
awpy>=2.0.0

# LLM providers (at least one required)
google-generativeai==0.8.0  # Gemini (legacy SDK for video analysis)
google-genai>=1.55.0        # Gemini (new SDK for TTS + Interactions API)
anthropic>=0.40.0           # Claude (optional)
openai>=1.50.0              # OpenAI (optional)

# Google Cloud
google-cloud-storage>=2.14.0
google-cloud-firestore>=2.14.0
google-cloud-texttospeech>=2.14.0

# Utilities
pydantic>=2.9.0
python-dotenv==1.0.0
